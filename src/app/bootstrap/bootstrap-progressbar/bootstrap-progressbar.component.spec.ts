/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BootstrapProgressbarComponent } from './bootstrap-progressbar.component';

describe('Component: BootstrapProgressbar', () => {
  it('should create an instance', () => {
    let component = new BootstrapProgressbarComponent();
    expect(component).toBeTruthy();
  });
});
