/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BootstrapCollapseComponent } from './bootstrap-collapse.component';

describe('Component: BootstrapCollapse', () => {
  it('should create an instance', () => {
    let component = new BootstrapCollapseComponent();
    expect(component).toBeTruthy();
  });
});
