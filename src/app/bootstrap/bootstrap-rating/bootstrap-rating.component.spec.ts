/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BootstrapRatingComponent } from './bootstrap-rating.component';

describe('Component: BootstrapRating', () => {
  it('should create an instance', () => {
    let component = new BootstrapRatingComponent();
    expect(component).toBeTruthy();
  });
});
