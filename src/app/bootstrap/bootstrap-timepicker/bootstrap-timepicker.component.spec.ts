/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BootstrapTimepickerComponent } from './bootstrap-timepicker.component';

describe('Component: BootstrapTimepicker', () => {
  it('should create an instance', () => {
    let component = new BootstrapTimepickerComponent();
    expect(component).toBeTruthy();
  });
});
